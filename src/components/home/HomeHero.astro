---
import GetStarted from "@/components/GetStarted.astro";
import Rebound from "@/components/Rebound.astro";
---

<section class="homeHero">
  <div class="container">
    <Rebound size="7.2rem" />
    <h1 class="text-center">
      The open source toolbox <br /> for <span class="accident">resilient</span>
      operations
    </h1>
    <p class="text-center">
      Rebound is a set of tools, ranging from a network injection fault proxy to
      a reliability automation platform, so people can rely on you being there
      when they need.
    </p>
    <div class="homeHero__start">
      <a href="https://github.com/rebound-how/" class="button button--primary"
        >Read the docs</a
      >
      <span>or</span>
      <GetStarted />
    </div>
  </div>
</section>

<style lang="scss">
  .homeHero {
    display: grid;
    place-content: center;
    min-height: 70vh;

    h1 {
      font-weight: 700;

      .accident {
        display: inline-block;

        animation: 1s bounce-down forwards 1s;
        transform-origin: 0% 100%;
      }

      &:hover {
        .accident {
          animation: 0.3s bounce-up forwards;
        }
      }
    }

    p {
      max-width: 60ch;

      color: var(--text-color-dim);
      font-size: 2.4rem;
    }

    &__start {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: var(--space-small);
      margin-top: var(--space-medium);

      @media (width > 50rem) {
        flex-direction: row;
      }
    }
  }

  @keyframes bounce-down {
    0% {
      transform: rotate(0);
    }

    20% {
      transform: rotate(3deg);
    }

    30% {
      transform: rotate(1.5deg);
    }

    40% {
      transform: rotate(3deg);
    }

    50% {
      transform: rotate(2.2deg);
    }

    60% {
      transform: rotate(3deg);
    }

    70% {
      transform: rotate(2.5deg);
    }

    80% {
      transform: rotate(3deg);
    }

    90% {
      transform: rotate(2.8deg);
    }

    100% {
      transform: rotate(3deg);
    }
  }

  @keyframes bounce-up {
    0% {
      transform: rotate(3deg);
    }

    100% {
      transform: rotate(0);
    }
  }
</style>
